/**
 * SVG icons for dsh-msg9-kit's browser face (lucide-react, bundled into
 * lib/client.js — no runtime dependency). All stroked in `currentColor`, so
 * they follow the surrounding palette and both shell themes.
 *
 * @module dsh-msg9-kit/client-icons
 */

import {
  ArrowRight,
  Bell,
  BellOff,
  Check,
  ChevronDown,
  ChevronRight,
  Copy,
  Globe,
  Inbox,
  Mail,
  MessagesSquare,
  PenLine,
  RefreshCw,
  Reply,
  Search,
  Send,
  Trash2,
  UserPlus,
  Users,
  X,
} from 'lucide-react'

export { ArrowRight, Bell, BellOff, Check, ChevronDown, ChevronRight, Copy, Globe, Inbox, Mail, MessagesSquare, PenLine, RefreshCw, Reply, Search, Send, Trash2, UserPlus, Users, X }
